# Face Tracker Update Checklist

Issue #1281 の Web カメラ顔トラッキング UI / 挙動変更に対する動作確認メモ。

## WPF UI

- [ ] Face Tracker パネルで、従来の `Webカメラ (軽量)` / `Webカメラ (高品質)` の選択 UI が表示されないこと。
- [ ] Face Tracker パネルで、Web カメラ / 外部トラッキングの切り替えが意図した表示になっていること。
- [ ] `表情をカメラでトラッキング` チェックボックスが表示されること。
- [ ] `表情をカメラでトラッキング` が OFF のとき、配下の表情トラッキング依存 UI が disabled になること。
- [ ] `表情をカメラでトラッキング` が OFF のとき、表情トラッキング依存機能が使えない旨の注意表示が出ること。
- [ ] `表情をカメラでトラッキング` が ON のとき、頬 / 舌が使えない旨の注意表示が出ること。
- [ ] `カメラで検出したまばたきを適用` が表示され、まばたき設定ボタンがその右にあること。
- [ ] `カメラで検出したリップシンクを適用` が表情トラッキング配下に表示されること。
- [ ] `パーフェクトシンクを使用` とヘルプ導線が表情トラッキング配下に表示されること。
- [ ] リップシンク用マイク一覧が表示され、選択できること。
- [ ] `詳細` Expander の見栄えとサイズ感に違和感がないこと。
- [ ] `詳細` Expander 内に `動きをすばやく反映` が表示されること。
- [ ] `詳細` Expander 内に `手前・奥への移動を有効化` が表示されること。
- [ ] `詳細` Expander 内に `マウスポインタのほうを見るかどうか` ComboBox が表示されること。
- [ ] `マウスポインタのほうを見るかどうか` ComboBox の表示順が `つねに見る` / `カメラがオフのときは見る` / `つねに見ない` であること。
- [ ] 詳細設定画面側に旧 LookAt 3 択 UI が表示されないこと。
- [ ] `現在位置で顔をキャリブレーション` ボタンが Web カメラ設定内に残っていること。

## Settings And Persistence

- [ ] `表情をカメラでトラッキング` の ON/OFF が保存され、再起動後も復元されること。
- [ ] `カメラで検出したまばたきを適用` の ON/OFF が保存され、再起動後も復元されること。
- [ ] `動きをすばやく反映` の ON/OFF が保存され、再起動後も復元されること。
- [ ] `マウスポインタのほうを見るかどうか` の選択値が保存され、再起動後も復元されること。
- [ ] リップシンク用マイク選択が保存され、再起動後も復元されること。
- [ ] 既存設定ファイルから起動した場合に、互換名として残した設定が読めること。
- [ ] 顔トラッキング基本設定のリセットで、新規項目が期待するデフォルトに戻ること。
- [ ] Web カメラ詳細設定のリセットで、新規項目が期待するデフォルトに戻ること。

## Unity Behavior

- [ ] Web カメラ顔トラッキング ON で、頭部トラッキングが動作すること。
- [ ] `表情をカメラでトラッキング` OFF では、MediaPipe の BlendShape 出力が使われないこと。
- [ ] `表情をカメラでトラッキング` ON では、MediaPipe の BlendShape 出力が使われること。
- [ ] `表情をカメラでトラッキング` の ON/OFF 変更時に、必要に応じて MediaPipe タスクが再起動されること。
- [ ] `カメラで検出したまばたきを適用` ON では、Web カメラ由来のまばたきが適用されること。
- [ ] `カメラで検出したまばたきを適用` OFF では、表情トラッキング ON でも自動まばたきに戻ること。
- [ ] `表情をカメラでトラッキング` OFF かつ `カメラで検出したまばたきを適用` ON では、自動まばたきが適用されること。
- [ ] `カメラで検出したリップシンクを適用` ON では、Web カメラ由来のリップシンクが優先されること。
- [ ] `カメラで検出したリップシンクを適用` OFF でも、表情トラッキング ON なら BlendShape 出力自体は維持されること。
- [ ] `表情をカメラでトラッキング` OFF かつ `カメラで検出したリップシンクを適用` ON では、Web カメラ由来のリップシンクが適用されないこと。
- [ ] `パーフェクトシンクを使用` ON で、Web カメラ表情トラッキング中に Perfect Sync が適用されること。
- [ ] `パーフェクトシンクを使用` OFF で、Web カメラ表情トラッキング中でも Perfect Sync が適用されないこと。
- [ ] `表情をカメラでトラッキング` OFF かつ `パーフェクトシンクを使用` ON では、Web カメラ由来の Perfect Sync が適用されないこと。
- [ ] Face Switch が `表情をカメラでトラッキング` ON のときだけ Web カメラ BlendShape 基準で動作すること。
- [ ] `動きをすばやく反映` ON で、頭部回転と体オフセットの反応が速い側になること。
- [ ] `動きをすばやく反映` OFF で、頭部回転と体オフセットの反応が遅い側になること。
- [ ] `手前・奥への移動を有効化` ON/OFF が Web カメラ由来の Z 方向オフセットに反映されること。

## Mouse Look-At

- [ ] `マウスポインタのほうを見るかどうか = つねに見る` で、Web カメラ顔トラッキング中でもマウスポインタ LookAt が有効になること。
- [ ] `マウスポインタのほうを見るかどうか = カメラがオフのときは見る` で、Web カメラ顔トラッキング中はマウスポインタ LookAt が無効になること。
- [ ] `マウスポインタのほうを見るかどうか = カメラがオフのときは見る` で、Web カメラ顔トラッキング OFF 中はマウスポインタ LookAt が有効になること。
- [ ] `マウスポインタのほうを見るかどうか = つねに見ない` で、常にマウスポインタ LookAt が無効になること。
- [ ] Web カメラ中にマウスポインタ LookAt を有効にした場合、MediaPipe の目線検出と二重に適用されないこと。
- [ ] 外部トラッキング中は従来どおり LookAt が無効になること。
- [ ] VMC Protocol の頭部トラッキング中は従来どおり LookAt が無効になること。
- [ ] ゲーム入力の三人称移動中は従来どおり LookAt が無効になること。
- [ ] 旧 `ユーザーを見る` 相当の MainCamera LookAt が新 UI から操作できないこと。

## Device Selection

- [ ] Web カメラデバイス一覧が表示され、選択したカメラでトラッキングが開始されること。
- [ ] 空のカメラデバイス名が一時的に出ても、不自然に設定へ同期されないこと。
- [ ] マイクデバイス一覧が表示され、選択したマイクでリップシンクが動作すること。
- [ ] 空のマイクデバイス名が一時的に出ても、不自然に設定へ同期されないこと。

## Regression Checks

- [ ] 外部トラッキングを有効にしたとき、Web カメラ顔トラッキング側が競合しないこと。
- [ ] 画像ベースの手トラッキングと Web カメラ顔トラッキングの併用が従来どおり動作すること。
- [ ] ハンドトラッキングのみ / 顔のみ / 顔 + 手の MediaPipe タスク切り替えが従来どおり動作すること。
- [ ] 顔キャリブレーションが Web カメラ顔トラッキング中に実行できること。
- [ ] 顔キャリブレーション結果が保存され、再起動後に反映されること。
- [ ] 日英 UI 文言に旧 `Webカメラ (軽量)` / `Webカメラ (高品質)` 相当の文言が残っていないこと。
- [ ] 中国語 UI で削除済み項目の古い文言が表示されないこと。

## Build And Test

- [ ] WPF プロジェクトがビルドできること。
- [ ] WPF テストプロジェクトが成功すること。
- [ ] Unity プロジェクトがコンパイルできること。
